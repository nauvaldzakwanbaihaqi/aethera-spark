# Aethera Spark v1.0 - Environment Variables Example

# Optional: Delay override (in milliseconds) for mock API responses (auth & AI generation).
# Leave empty or omit for realistic demo delays (800ms - 2000ms).
# Set to a lower number (e.g. 100) during automated E2E testing.
VITE_MOCK_DELAY_MS=
